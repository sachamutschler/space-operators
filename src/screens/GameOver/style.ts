import styled from "styled-components/native";

export const SpaceShipImage = styled.Image`

    width: 560px;
    height: 560px; 
    transform: rotate(345deg);
    position: relative;
        top: -40px;
        right: 100px;
`;